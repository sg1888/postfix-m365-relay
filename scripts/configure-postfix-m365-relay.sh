#!/usr/bin/env bash
echo 'configure-postfix-m365-relay.sh retired: config is rendered inside the image at boot.' >&2
echo 'See build/postfix-m365-relay/render-config.sh and docs/CONFIGURATION.md.' >&2
exit 1
